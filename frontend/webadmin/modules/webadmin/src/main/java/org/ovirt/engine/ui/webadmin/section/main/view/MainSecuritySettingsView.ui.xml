<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder
    xmlns:ui="urn:ui:com.google.gwt.uibinder"
    xmlns:g="urn:import:com.google.gwt.user.client.ui">

    <ui:style>
        .mainContainer {
            display: table;
            width: 100%;
            height: 800px;
            background-color: #f5f5f5;
        }

        .sidebar {
            display: table-cell;
            width: 250px;
            height: 800px;
            vertical-align: top;
            background-color: #ffffff;
            border-right: 1px solid #ddd;
        }

        .sidebarContent {
            height: 100%;
            overflow-y: auto;
        }

        .sidebarTitle {
            padding: 15px 20px;
            font-size: 18px;
            font-weight: bold;
            border-bottom: 1px solid #ddd;
            background-color: #f8f8f8;
        }

        .menuList {
            display: block;
        }

        .menuItem {
            display: block;
            padding: 12px 20px;
            cursor: pointer;
            border-bottom: 1px solid #f0f0f0;
            font-size: 14px;
            transition: background-color 0.2s;
            line-height: 1.5;
        }

        .menuItem:hover {
            background-color: #f8f8f8;
        }

        .menuItemActive {
            background-color: #337ab7;
            color: white;
            font-weight: bold;
        }

        .menuItemActive:hover {
            background-color: #286090;
        }

        .contentArea {
            display: table-cell;
            vertical-align: top;
            background-color: #ffffff;
            padding: 0;
            height: 800px;
        }

        .viewContainer {
            width: 100%;
            height: 100%;
        }
    </ui:style>

    <g:HTMLPanel addStyleNames="{style.mainContainer}">
        <div class="{style.sidebar}">
            <div class="{style.sidebarContent}">
                <div class="{style.sidebarTitle}">보안 설정</div>
                <div class="{style.menuList}">
                    <div ui:field="integrityCheckMenuItem" class="{style.menuItem} {style.menuItemActive}">무결성 검사</div>
                    <div ui:field="clientManagementMenuItem" class="{style.menuItem}">클라이언트 관리</div>
                    <div ui:field="auditLogManagementMenuItem" class="{style.menuItem}">감사기록 관리</div>
                </div>
            </div>
        </div>
        <div class="{style.contentArea}">
            <g:SimplePanel ui:field="contentPanel" addStyleNames="{style.viewContainer}"/>
        </div>
    </g:HTMLPanel>
</ui:UiBinder>
